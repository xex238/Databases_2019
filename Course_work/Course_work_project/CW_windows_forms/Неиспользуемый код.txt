            //// Формирование строки для вставки в таблицу "Билет"
            //flight_dataGridView = new DataGridView();
            ////flight_dataGridView = airport_dataGridView; // Копирование по ссылке

            //MessageBox.Show(flight_dataGridView[0, 0].Value.ToString(), "Первая ячейка в таблице рейсов", MessageBoxButtons.OK, MessageBoxIcon.Error);

            //flight_dataGridView.Columns.Clear();

            //DataGridViewTextBoxColumn[] columns = new DataGridViewTextBoxColumn[count_of_columns];
            //for (int i = 0; i < count_of_columns; i++)
            //{
            //    columns[i] = new DataGridViewTextBoxColumn();
            //    columns[i].ReadOnly = true;
            //    columns[i].HeaderText = airport_dataGridView.Columns[i].HeaderText;
            //}